/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SafeHtmlMatTooltipDirective } from './safe-html-mat-tooltip.directive';

describe('Directive: SafeHtmlMatTooltip', () => {
  it('should create an instance', () => {
    const directive = new SafeHtmlMatTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
